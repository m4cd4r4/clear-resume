// SessionStart hook: loads the waiting handover for this repo.
// Scaffold only - the loader lands in the next build step. Exits quietly so an
// installed-but-unfinished plugin never blocks or pollutes a session.
process.exit(0);
